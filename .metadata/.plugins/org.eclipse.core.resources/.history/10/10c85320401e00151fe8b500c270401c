package conversion;

import main.DfmObject;

public abstract class AConversionRule {
    public void apply(DfmObject dfmObject) {
        if (!isApplicable(dfmObject))
            return;
        
        doApply(dfmObject);
    }
    
    public virtual isApplicable(dfmObject);
    
}
