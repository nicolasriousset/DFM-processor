package conversion;

public class Utils {
    static String addInt(String sVal, int increment) {
        int val = 0;
        if (sVal != null && !sVal.isEmpty())
            val = Integer.parseInt(sVal);
        val += increment;
        return String.valueOf(val); 
    }
}
